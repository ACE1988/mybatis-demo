<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.demo.dao.SystemVariableMapper">

    <select id="queryByCode" parameterType="String" resultType="com.mybatis.demo.entity.SystemVariable">
        SELECT * FROM t_system_variable WHERE code = #{code}
    </select>

    <select id="queryAll" resultType="com.mybatis.demo.entity.SystemVariable">
        SELECT * FROM t_system_variable WHERE ;
    </select>
</mapper>